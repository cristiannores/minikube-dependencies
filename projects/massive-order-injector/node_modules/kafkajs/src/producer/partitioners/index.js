const DefaultPartitioner = require('kafkajs/src/producer/partitioners/default')
const JavaCompatiblePartitioner = require('kafkajs/src/producer/partitioners/defaultJava')

module.exports = {
  DefaultPartitioner,
  JavaCompatiblePartitioner,
}
