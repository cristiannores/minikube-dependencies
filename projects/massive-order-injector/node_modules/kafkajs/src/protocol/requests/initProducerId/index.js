const versions = {
  0: ({ transactionalId, transactionTimeout = 5000 }) => {
    const request = require('kafkajs/src/protocol/requests/initProducerId/v0/request')
    const response = require('kafkajs/src/protocol/requests/initProducerId/v0/response')
    return { request: request({ transactionalId, transactionTimeout }), response }
  },
}

module.exports = {
  versions: Object.keys(versions),
  protocol: ({ version }) => versions[version],
}
