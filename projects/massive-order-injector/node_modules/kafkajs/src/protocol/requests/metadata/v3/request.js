const requestV0 = require('kafkajs/src/protocol/requests/metadata/v0/request')

/**
 * Metadata Request (Version: 3) => [topics]
 *   topics => STRING
 */

module.exports = ({ topics }) => Object.assign(requestV0({ topics }), { apiVersion: 3 })
