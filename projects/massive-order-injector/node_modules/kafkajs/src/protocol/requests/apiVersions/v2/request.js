const requestV0 = require('kafkajs/src/protocol/requests/apiVersions/v0/request')

// ApiVersions Request after v1 indicates the client can parse throttle_time_ms

module.exports = () => ({ ...requestV0(), apiVersion: 2 })
