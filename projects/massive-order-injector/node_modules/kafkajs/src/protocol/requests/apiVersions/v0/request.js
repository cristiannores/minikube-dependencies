const Encoder = require('kafkajs/src/protocol/encoder')
const { ApiVersions: apiKey } = require('kafkajs/src/protocol/requests/apiKeys')

/**
 * ApiVersionRequest => ApiKeys
 */

module.exports = () => ({
  apiKey,
  apiVersion: 0,
  apiName: 'ApiVersions',
  encode: async () => new Encoder(),
})
